---
phase: 02-capability-detection-runtime-guardrails
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/kernel/machine/mod.rs
  - src/kernel/machine/cpu.rs
  - src/kernel/klib/mod.rs
  - src/kernel/klib/simd.rs
  - src/kernel/services/mod.rs
  - src/kernel/services/simd.rs
autonomous: true
requirements: [COMP-03, HW-01]
user_setup: []
must_haves:
  truths:
    - "The repo has a canonical low-level SIMD capability detection surface and a canonical runtime-policy storage/query surface."
  artifacts:
    - "src/kernel/machine/cpu.rs"
    - "src/kernel/klib/simd.rs"
    - "src/kernel/services/simd.rs"
  key_links:
    - "services::simd -> machine::cpu for detection"
    - "services::simd -> klib::simd for policy installation"
---

<objective>
Create the foundational Phase 2 surfaces for CPU SIMD capability detection and runtime-policy storage.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Add typed CPU capability detection under machine</name>
  <files>src/kernel/machine/mod.rs, src/kernel/machine/cpu.rs</files>
  <action>Add a typed `machine::cpu` module that can detect CPUID availability and parse MMX/SSE/SSE2 capability bits without exposing service or core policy logic.</action>
  <verify>rg -n "SimdCapabilities|cpuid|sse2|mmx" src/kernel/machine</verify>
</task>

<task type="auto">
  <name>Task 2: Add runtime-policy storage under klib</name>
  <files>src/kernel/klib/mod.rs, src/kernel/klib/simd.rs</files>
  <action>Add a `klib::simd` policy surface that stores the currently installed runtime policy, exposes pure queries, and defaults to scalar-only execution.</action>
  <verify>rg -n "RuntimePolicy|mmx_allowed|sse_allowed|sse2_allowed" src/kernel/klib</verify>
</task>

<task type="auto">
  <name>Task 3: Add a service seam that bridges detection into policy installation</name>
  <files>src/kernel/services/mod.rs, src/kernel/services/simd.rs</files>
  <action>Add a `services::simd` module that detects capabilities via `machine::cpu`, translates them into a scalar-only runtime policy for now, and installs that policy into `klib::simd`.</action>
  <verify>rg -n "initialize_runtime_policy|install_policy|machine::cpu|klib::simd" src/kernel/services</verify>
</task>

</tasks>

<output>
After completion, create `.planning/phases/02-capability-detection-runtime-guardrails/02-01-SUMMARY.md`
</output>
