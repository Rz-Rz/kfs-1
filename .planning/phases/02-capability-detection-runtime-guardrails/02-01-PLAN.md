---
phase: 02-capability-detection-runtime-guardrails
plan: 01
type: execute
wave: 1
depends_on: ["01-03"]
files_modified:
  [
    src/kernel/machine/mod.rs,
    src/kernel/machine/cpu.rs,
    src/kernel/klib/mod.rs,
    src/kernel/klib/simd.rs,
    tests/host_simd_policy.rs,
    scripts/tests/unit/simd-policy.sh,
  ]
autonomous: true
requirements: [COMP-03, HW-01]
user_setup: []
must_haves:
  truths:
    - "The repo has a canonical, host-testable SIMD capability and policy surface that can represent unsupported hardware and scalar fallback."
  artifacts:
    - "src/kernel/machine/cpu.rs"
    - "src/kernel/klib/simd.rs"
    - "tests/host_simd_policy.rs"
  key_links:
    - "src/kernel/machine/cpu.rs -> src/kernel/klib/simd.rs via typed feature/policy handoff"
---

<objective>
Create the typed CPU capability probe and pure SIMD policy surface for the branch.

Purpose: Give later phases one canonical capability/policy API instead of scattering raw checks.
Output: A new `machine::cpu` primitive layer, a new `klib::simd` policy module, and host tests for decode and fallback behavior.
</objective>

<context>
@.planning/phases/02-capability-detection-runtime-guardrails/02-CONTEXT.md
@.planning/phases/02-capability-detection-runtime-guardrails/02-RESEARCH.md
@docs/simd_policy.md
@src/kernel/machine/mod.rs
@src/kernel/klib/mod.rs
@tests/host_memory.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add typed CPU capability primitives</name>
  <files>src/kernel/machine/mod.rs, src/kernel/machine/cpu.rs</files>
  <action>Add a new `machine::cpu` module with typed SIMD capability structs, pure feature-bit decoding helpers, and the runtime probe surface needed by later phases. Keep ownership primitive-only and avoid upward imports.</action>
  <verify>rg -n "pub mod cpu|Simd|cpuid|feature" src/kernel/machine/mod.rs src/kernel/machine/cpu.rs</verify>
</task>

<task type="auto">
  <name>Task 2: Add canonical klib SIMD policy state</name>
  <files>src/kernel/klib/mod.rs, src/kernel/klib/simd.rs</files>
  <action>Add a `klib::simd` module that owns pure policy selection and installed runtime-policy state. It must represent unsupported hardware, disabled policy, and scalar fallback without depending on higher layers.</action>
  <verify>rg -n "Scalar|Unsupported|Policy|install|current" src/kernel/klib/mod.rs src/kernel/klib/simd.rs</verify>
</task>

<task type="auto">
  <name>Task 3: Add host policy tests</name>
  <files>tests/host_simd_policy.rs, scripts/tests/unit/simd-policy.sh</files>
  <action>Add host tests that validate feature decoding and scalar fallback policy through the real crate API. Follow the existing shell-wrapper discovery contract.</action>
  <verify>bash scripts/tests/unit/simd-policy.sh i386 host-simd-policy-unit-tests-pass</verify>
</task>

</tasks>

<verification>
- [ ] `bash scripts/tests/unit/simd-policy.sh i386 host-simd-policy-unit-tests-pass`
</verification>

<output>
After completion, create `.planning/phases/02-capability-detection-runtime-guardrails/02-01-SUMMARY.md`
</output>
