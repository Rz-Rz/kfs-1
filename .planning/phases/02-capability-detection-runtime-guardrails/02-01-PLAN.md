---
phase: 02-capability-detection-runtime-guardrails
plan: 01
type: execute
wave: 1
depends_on: ["01-03"]
files_modified:
  - src/arch/i386/runtime_io.asm
  - src/kernel/core/mod.rs
  - src/kernel/core/entry.rs
  - src/kernel/core/init.rs
  - src/kernel/core/simd.rs
autonomous: true
requirements: [HW-01, HW-03]
user_setup: []
must_haves:
  truths:
    - "The kernel can observe CPUID presence and MMX/SSE/SSE2 capability without executing accelerated helper code."
    - "Core early-init emits a canonical scalar-policy result even when capability exists."
  artifacts:
    - "src/arch/i386/runtime_io.asm"
    - "src/kernel/core/simd.rs"
    - "src/kernel/core/init.rs"
  key_links:
    - "src/kernel/core/entry.rs -> src/arch/i386/runtime_io.asm via kfs_arch_* CPUID helpers"
    - "src/kernel/core/init.rs -> src/kernel/core/simd.rs via early-init policy reporting"
---

<objective>
Add the capability-detection and runtime-policy logic at the existing arch/core boundary.

Purpose: Make SIMD/MMX/SSE2 support observable while still forcing scalar behavior in Phase 2.
Output: Arch helpers, a core SIMD policy module, and runtime markers emitted from early init.
</objective>

<execution_context>
@~/.codex/get-shit-done/workflows/execute-plan.md
@~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-capability-detection-runtime-guardrails/02-CONTEXT.md
@.planning/phases/02-capability-detection-runtime-guardrails/02-RESEARCH.md
@docs/simd_policy.md
@src/arch/i386/runtime_io.asm
@src/kernel/core/entry.rs
@src/kernel/core/init.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add arch CPUID helpers and core SIMD policy reporting</name>
  <files>src/arch/i386/runtime_io.asm, src/kernel/core/mod.rs, src/kernel/core/entry.rs, src/kernel/core/init.rs, src/kernel/core/simd.rs</files>
  <read_first>src/arch/i386/runtime_io.asm, src/kernel/core/entry.rs, src/kernel/core/init.rs, docs/simd_policy.md</read_first>
  <action>Extend the existing `kfs_arch_*` runtime helper surface with safe CPUID support/capability access, add a core-owned SIMD policy module, and call it from early init. The resulting policy must remain scalar-only in Phase 2, but test mode must be able to observe CPUID presence plus MMX/SSE/SSE2 capability state and the final scalar-policy decision.</action>
  <verify>rg -n "kfs_arch_.*cpuid|SIMD_CPUID|SIMD_POLICY_SCALAR|MMX|SSE2" src/arch/i386/runtime_io.asm src/kernel/core/entry.rs src/kernel/core/init.rs src/kernel/core/simd.rs</verify>
  <acceptance_criteria>
    - CPUID helpers exist at the arch/runtime-helper boundary
    - early init reports canonical SIMD capability/policy markers
    - Phase 2 policy stays scalar-only even on capable CPUs
  </acceptance_criteria>
  <done>The repo can observe capability safely without executing accelerated code.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `rg -n "kfs_arch_.*cpuid|SIMD_CPUID|SIMD_POLICY_SCALAR|MMX|SSE2" src/arch/i386/runtime_io.asm src/kernel/core/entry.rs src/kernel/core/init.rs src/kernel/core/simd.rs`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Capability detection is observable and acceleration still remains disabled
</success_criteria>

<output>
After completion, create `.planning/phases/02-capability-detection-runtime-guardrails/02-01-SUMMARY.md`
</output>
