---
phase: 02-capability-detection-runtime-guardrails
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  [
    src/kernel/services/mod.rs,
    src/kernel/services/simd.rs,
    src/kernel/core/init.rs,
    docs/kernel_architecture.md,
    docs/simd_policy.md,
  ]
autonomous: true
requirements: [COMP-03, HW-01, HW-03]
user_setup: []
must_haves:
  truths:
    - "Early init installs a canonical SIMD runtime policy through the service layer before future accelerated helper paths would become reachable."
  artifacts:
    - "src/kernel/services/simd.rs"
    - "src/kernel/core/init.rs"
  key_links:
    - "src/kernel/core/init.rs -> src/kernel/services/simd.rs via early-init sequencing"
    - "src/kernel/services/simd.rs -> src/kernel/klib/simd.rs via installed runtime policy"
---

<objective>
Wire capability detection into early init through the allowed layer boundary.

Purpose: Make the policy surface real in runtime sequencing while preserving the repo's dependency rules.
Output: A new `services::simd` facade, early-init policy installation, and matching live-doc updates.
</objective>

<context>
@.planning/phases/02-capability-detection-runtime-guardrails/02-CONTEXT.md
@.planning/phases/02-capability-detection-runtime-guardrails/02-RESEARCH.md
@.planning/phases/02-capability-detection-runtime-guardrails/02-01-SUMMARY.md
@src/kernel/core/init.rs
@src/kernel/services/mod.rs
@docs/kernel_architecture.md
@docs/simd_policy.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add service-layer SIMD policy orchestration</name>
  <files>src/kernel/services/mod.rs, src/kernel/services/simd.rs</files>
  <action>Add a `services::simd` module that probes machine features, installs scalar-safe policy into `klib`, and returns deterministic status for early init.</action>
  <verify>rg -n "machine::cpu|klib::simd|install|policy" src/kernel/services/mod.rs src/kernel/services/simd.rs</verify>
</task>

<task type="auto">
  <name>Task 2: Sequence SIMD policy installation in early init</name>
  <files>src/kernel/core/init.rs</files>
  <action>Update early init to install the runtime SIMD policy before the memory-helper sanity path, emit deterministic diagnostics markers in test mode, and fail cleanly if policy installation cannot proceed.</action>
  <verify>rg -n "SIMD|simd::|EarlyInitFailure" src/kernel/core/init.rs</verify>
</task>

<task type="auto">
  <name>Task 3: Align live docs to the new ownership path</name>
  <files>docs/kernel_architecture.md, docs/simd_policy.md</files>
  <action>Document that runtime SIMD policy orchestration now lives in `services`, machine probing lives in `machine`, and durable policy state lives in `klib`.</action>
  <verify>rg -n "services::simd|machine::cpu|klib::simd" docs/kernel_architecture.md docs/simd_policy.md</verify>
</task>

</tasks>

<verification>
- [ ] `rg -n "services::simd|machine::cpu|klib::simd|SIMD_POLICY" src/kernel/core/init.rs src/kernel/services/mod.rs src/kernel/services/simd.rs docs/kernel_architecture.md docs/simd_policy.md`
</verification>

<output>
After completion, create `.planning/phases/02-capability-detection-runtime-guardrails/02-02-SUMMARY.md`
</output>
