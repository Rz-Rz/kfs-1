---
phase: 02-capability-detection-runtime-guardrails
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/kernel/core/init.rs
  - src/kernel/core/entry.rs
  - src/arch/i386/runtime_io.asm
  - Makefile
requirements: [COMP-03, HW-01, HW-03]
user_setup: []
must_haves:
  truths:
    - "Early init installs runtime SIMD policy before normal flow and keeps acceleration disabled when unsupported or forced off."
  artifacts:
    - "src/kernel/core/init.rs"
    - "src/arch/i386/runtime_io.asm"
    - "Makefile"
  key_links:
    - "core::init -> services::simd via early init sequencing"
    - "runtime_io.asm -> machine/services policy overrides via test hooks"
---

<objective>
Wire the runtime-policy initialization into early init and add deterministic test override hooks.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Sequence runtime-policy initialization in early init</name>
  <files>src/kernel/core/init.rs</files>
  <action>Call the new service seam during early init and emit deterministic runtime markers in test mode so later tests can observe the policy state.</action>
  <verify>rg -n "SIMD_|initialize_runtime_policy" src/kernel/core/init.rs src/kernel/services/simd.rs</verify>
</task>

<task type="auto">
  <name>Task 2: Add deterministic arch override hooks for no-cpuid and forced-scalar cases</name>
  <files>src/kernel/core/entry.rs, src/arch/i386/runtime_io.asm, Makefile</files>
  <action>Extend the existing `kfs_arch_*` test-hook pattern and Makefile env plumbing so runtime tests can force no-CPUID and forced-disable paths.</action>
  <verify>rg -n "NO_CPUID|DISABLE_SIMD|kfs_arch_should" Makefile src/kernel/core/entry.rs src/arch/i386/runtime_io.asm</verify>
</task>

</tasks>

<output>
After completion, create `.planning/phases/02-capability-detection-runtime-guardrails/02-02-SUMMARY.md`
</output>
