---
phase: 02-capability-detection-runtime-guardrails
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/kernel/klib/memory/mod.rs
  - tests/host_memory.rs
  - scripts/tests/unit/memory-helpers.sh
autonomous: true
requirements: [COMP-03]
user_setup: []
must_haves:
  truths:
    - "The memory helper facade has one canonical acceleration guardrail seam and Phase 2 keeps it on the scalar path."
  artifacts:
    - "src/kernel/klib/memory/mod.rs"
    - "tests/host_memory.rs"
    - "scripts/tests/unit/memory-helpers.sh"
  key_links:
    - "src/kernel/klib/memory/mod.rs -> src/kernel/klib/memory/imp.rs via scalar-only facade"
    - "scripts/tests/unit/memory-helpers.sh -> tests/host_memory.rs via guardrail-aware host checks"
---

<objective>
Add the canonical guardrail seam for future accelerated memory helpers without changing current scalar behavior.

Purpose: Give later accelerated helper work one approved decision point instead of scattering policy logic.
Output: Guardrail-aware memory facade plus unit/source tests that prove the scalar path remains canonical in Phase 2.
</objective>

<execution_context>
@~/.codex/get-shit-done/workflows/execute-plan.md
@~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-capability-detection-runtime-guardrails/02-CONTEXT.md
@.planning/phases/02-capability-detection-runtime-guardrails/02-RESEARCH.md
@.planning/phases/02-capability-detection-runtime-guardrails/02-01-SUMMARY.md
@src/kernel/klib/memory/mod.rs
@src/kernel/klib/memory/imp.rs
@tests/host_memory.rs
@scripts/tests/unit/memory-helpers.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add canonical scalar guardrail seam to memory facade</name>
  <files>src/kernel/klib/memory/mod.rs, tests/host_memory.rs, scripts/tests/unit/memory-helpers.sh</files>
  <read_first>src/kernel/klib/memory/mod.rs, src/kernel/klib/memory/imp.rs, tests/host_memory.rs, scripts/tests/unit/memory-helpers.sh</read_first>
  <action>Add one canonical guardrail seam to the memory facade so future accelerated helpers have a single policy decision point. Phase 2 must keep that seam scalar-only and prove it with host/unit or source-level tests rather than adding hidden alternate paths.</action>
  <verify>rg -n "acceleration|scalar|guardrail" src/kernel/klib/memory/mod.rs tests/host_memory.rs scripts/tests/unit/memory-helpers.sh</verify>
  <acceptance_criteria>
    - `src/kernel/klib/memory/mod.rs` has a canonical guardrail seam
    - current memory behavior remains scalar and host-safe
    - tests explicitly cover the guardrail seam instead of assuming it
  </acceptance_criteria>
  <done>The future accelerated memory work has one approved policy hook.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `rg -n "acceleration|scalar|guardrail" src/kernel/klib/memory/mod.rs tests/host_memory.rs scripts/tests/unit/memory-helpers.sh`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The scalar path remains canonical behind one explicit guardrail seam
</success_criteria>

<output>
After completion, create `.planning/phases/02-capability-detection-runtime-guardrails/02-02-SUMMARY.md`
</output>
