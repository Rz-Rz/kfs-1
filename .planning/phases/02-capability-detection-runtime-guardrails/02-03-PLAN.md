---
phase: 02-capability-detection-runtime-guardrails
plan: 03
type: execute
wave: 3
depends_on: ["02-01", "02-02"]
files_modified:
  - tests/host_simd_policy.rs
  - scripts/architecture-tests/runtime-ownership.sh
  - scripts/rejection-tests/runtime-ownership-rejections.sh
  - scripts/boot-tests/runtime-markers.sh
  - scripts/rejection-tests/runtime-init-rejections.sh
requirements: [COMP-03, HW-01, HW-03]
user_setup: []
must_haves:
  truths:
    - "Tests can observe initialized scalar-only runtime policy and deterministic forced-disable/unsupported paths."
  artifacts:
    - "tests/host_simd_policy.rs"
    - "scripts/architecture-tests/runtime-ownership.sh"
    - "scripts/rejection-tests/runtime-init-rejections.sh"
  key_links:
    - "runtime tests -> SIMD policy markers"
    - "host tests -> klib::simd policy queries"
---

<objective>
Add proof coverage for the new capability-detection and scalar-only guardrail surfaces.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Add host tests for capability and policy semantics</name>
  <files>tests/host_simd_policy.rs</files>
  <action>Add host-linked tests for pure capability parsing and runtime-policy queries, keeping the assertions deterministic and independent of the real host CPU model.</action>
  <verify>bash scripts/tests/unit/host-rust-lib.sh tests/host_simd_policy.rs</verify>
</task>

<task type="auto">
  <name>Task 2: Extend architecture/runtime ownership proofs</name>
  <files>scripts/architecture-tests/runtime-ownership.sh, scripts/rejection-tests/runtime-ownership-rejections.sh</files>
  <action>Extend runtime ownership tests so the init path must pass through the new SIMD policy seam and reject bypasses.</action>
  <verify>rg -n "simd|policy" scripts/architecture-tests/runtime-ownership.sh scripts/rejection-tests/runtime-ownership-rejections.sh</verify>
</task>

<task type="auto">
  <name>Task 3: Extend boot/rejection tests for deterministic runtime policy markers</name>
  <files>scripts/boot-tests/runtime-markers.sh, scripts/rejection-tests/runtime-init-rejections.sh</files>
  <action>Add runtime-marker and negative-path coverage for initialized scalar-only policy, no-CPUID, and forced-disable paths.</action>
  <verify>rg -n "SIMD_|NO_CPUID|DISABLE_SIMD" scripts/boot-tests/runtime-markers.sh scripts/rejection-tests/runtime-init-rejections.sh</verify>
</task>

</tasks>

<verification>
- [ ] `bash scripts/tests/unit/host-rust-lib.sh tests/host_simd_policy.rs`
- [ ] `make test-plain`
</verification>

<output>
After completion, create `.planning/phases/02-capability-detection-runtime-guardrails/02-03-SUMMARY.md`
</output>
