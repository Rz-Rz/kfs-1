---
phase: 02-capability-detection-runtime-guardrails
plan: 03
type: execute
wave: 3
depends_on: ["02-01", "02-02"]
files_modified:
  - scripts/boot-tests/simd-runtime.sh
  - scripts/rejection-tests/simd-runtime-rejections.sh
autonomous: true
requirements: [COMP-03, HW-03]
user_setup: []
must_haves:
  truths:
    - "Tests can observe capability markers and prove unsupported or disabled paths stay scalar."
    - "Phase 2 preserves the existing freestanding no-SIMD artifact policy."
  artifacts:
    - "scripts/boot-tests/simd-runtime.sh"
    - "scripts/rejection-tests/simd-runtime-rejections.sh"
    - "scripts/stability-tests/freestanding-simd.sh"
  key_links:
    - "scripts/boot-tests/simd-runtime.sh -> src/kernel/core/init.rs via runtime markers"
    - "scripts/rejection-tests/simd-runtime-rejections.sh -> src/arch/i386/runtime_io.asm via test-toggle overrides"
---

<objective>
Add the boot/rejection proofs that make the Phase 2 runtime policy observable and enforce scalar fallback.

Purpose: Prevent Phase 2 capability detection from becoming an invisible or untested branch.
Output: Runtime and rejection tests for CPUID/capability markers plus scalar-policy enforcement.
</objective>

<execution_context>
@~/.codex/get-shit-done/workflows/execute-plan.md
@~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-capability-detection-runtime-guardrails/02-CONTEXT.md
@.planning/phases/02-capability-detection-runtime-guardrails/02-RESEARCH.md
@.planning/phases/02-capability-detection-runtime-guardrails/02-01-SUMMARY.md
@.planning/phases/02-capability-detection-runtime-guardrails/02-02-SUMMARY.md
@src/arch/i386/runtime_io.asm
@src/kernel/core/init.rs
@scripts/boot-tests/memory-runtime.sh
@scripts/rejection-tests/memory-rejections.sh
@scripts/stability-tests/freestanding-simd.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add runtime and rejection proofs for scalar policy enforcement</name>
  <files>scripts/boot-tests/simd-runtime.sh, scripts/rejection-tests/simd-runtime-rejections.sh</files>
  <read_first>src/arch/i386/runtime_io.asm, src/kernel/core/init.rs, scripts/boot-tests/memory-runtime.sh, scripts/rejection-tests/memory-rejections.sh, scripts/stability-tests/freestanding-simd.sh</read_first>
  <action>Add a boot/runtime test script that asserts the new SIMD markers and scalar-policy result, plus a rejection test that forces unsupported/disabled cases through the existing arch test-toggle pattern. The tests must prove the kernel stays scalar and boot-safe rather than silently drifting toward unguarded acceleration.</action>
  <verify>bash scripts/boot-tests/simd-runtime.sh i386 runtime-confirms-scalar-policy && bash scripts/rejection-tests/simd-runtime-rejections.sh i386 forced-no-cpuid-stays-scalar && bash scripts/stability-tests/freestanding-simd.sh i386 default-freestanding-kernel-disables-simd-instructions</verify>
  <acceptance_criteria>
    - boot/runtime tests assert capability markers and scalar policy
    - rejection tests cover unsupported or disabled capability cases
    - the existing freestanding no-SIMD artifact policy still passes unchanged
  </acceptance_criteria>
  <done>Phase 2 policy is observable and guarded by the repo's normal proof style.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `bash scripts/boot-tests/simd-runtime.sh i386 runtime-confirms-scalar-policy`
- [ ] `bash scripts/rejection-tests/simd-runtime-rejections.sh i386 forced-no-cpuid-stays-scalar`
- [ ] `bash scripts/stability-tests/freestanding-simd.sh i386 default-freestanding-kernel-disables-simd-instructions`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Unsupported or disabled cases are explicitly proven to remain scalar
</success_criteria>

<output>
After completion, create `.planning/phases/02-capability-detection-runtime-guardrails/02-03-SUMMARY.md`
</output>
