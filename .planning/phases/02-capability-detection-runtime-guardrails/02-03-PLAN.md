---
phase: 02-capability-detection-runtime-guardrails
plan: 03
type: execute
wave: 3
depends_on: ["02-01", "02-02"]
files_modified:
  [
    scripts/architecture-tests/runtime-ownership.sh,
    scripts/rejection-tests/runtime-ownership-rejections.sh,
    scripts/boot-tests/runtime-markers.sh,
    scripts/boot-tests/memory-runtime.sh,
    scripts/tests/README.md,
  ]
autonomous: true
requirements: [COMP-03, HW-01, HW-03]
user_setup: []
must_haves:
  truths:
    - "The repo's tests can observe SIMD policy installation and reject bypasses around the new service-layer runtime guardrails."
  artifacts:
    - "scripts/architecture-tests/runtime-ownership.sh"
    - "scripts/rejection-tests/runtime-ownership-rejections.sh"
    - "scripts/boot-tests/runtime-markers.sh"
  key_links:
    - "scripts/boot-tests/runtime-markers.sh -> src/kernel/core/init.rs via ordered SIMD policy markers"
---

<objective>
Prove the new runtime guardrails through the existing shell-based test framework.

Purpose: Ensure Phase 2 is enforced by host, architecture, boot, and rejection tests instead of convention.
Output: Updated proof scripts and any guidance needed for the new test surface.
</objective>

<context>
@.planning/phases/02-capability-detection-runtime-guardrails/02-CONTEXT.md
@.planning/phases/02-capability-detection-runtime-guardrails/02-RESEARCH.md
@.planning/phases/02-capability-detection-runtime-guardrails/02-01-SUMMARY.md
@.planning/phases/02-capability-detection-runtime-guardrails/02-02-SUMMARY.md
@scripts/architecture-tests/runtime-ownership.sh
@scripts/rejection-tests/runtime-ownership-rejections.sh
@scripts/boot-tests/runtime-markers.sh
@scripts/boot-tests/memory-runtime.sh
@scripts/tests/README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend runtime ownership and rejection guards</name>
  <files>scripts/architecture-tests/runtime-ownership.sh, scripts/rejection-tests/runtime-ownership-rejections.sh</files>
  <action>Add explicit cases that prove core init delegates SIMD policy installation through the service layer and reject direct/bypassed ownership paths.</action>
  <verify>bash scripts/architecture-tests/runtime-ownership.sh i386 core-init-calls-services-simd</verify>
</task>

<task type="auto">
  <name>Task 2: Extend boot/runtime proofs for SIMD policy markers</name>
  <files>scripts/boot-tests/runtime-markers.sh, scripts/boot-tests/memory-runtime.sh</files>
  <action>Add ordered runtime-marker coverage that proves SIMD policy installation happens during early init before memory-helper success markers are emitted.</action>
  <verify>bash scripts/boot-tests/runtime-markers.sh i386 runtime-confirms-simd-policy</verify>
</task>

<task type="auto">
  <name>Task 3: Update test guidance for the new host script</name>
  <files>scripts/tests/README.md</files>
  <action>Document the new host-unit test surface only if the live guidance would otherwise omit a canonical script or naming rule implication.</action>
  <verify>rg -n "simd-policy|host policy" scripts/tests/README.md || true</verify>
</task>

</tasks>

<verification>
- [ ] `bash scripts/architecture-tests/runtime-ownership.sh i386 core-init-calls-services-simd`
- [ ] `bash scripts/boot-tests/runtime-markers.sh i386 runtime-confirms-simd-policy`
- [ ] `make test-plain`
</verification>

<output>
After completion, create `.planning/phases/02-capability-detection-runtime-guardrails/02-03-SUMMARY.md`
</output>
