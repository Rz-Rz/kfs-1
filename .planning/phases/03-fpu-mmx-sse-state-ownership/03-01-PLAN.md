---
phase: 03-fpu-mmx-sse-state-ownership
plan: 01
type: execute
wave: 1
depends_on: ["02-03"]
files_modified:
  - src/kernel/machine/cpu.rs
  - src/kernel/machine/fpu.rs
  - src/kernel/machine/mod.rs
  - src/kernel/services/simd.rs
  - src/kernel/core/init.rs
  - src/kernel/klib/simd.rs
autonomous: true
requirements: [HW-02]
user_setup: []
must_haves:
  truths:
    - "Early init owns the required FPU/MMX/SSE runtime state before helper self-checks run."
    - "The canonical runtime policy records that machine state is owned even while acceleration remains deferred."
  artifacts:
    - "src/kernel/machine/fpu.rs"
    - "src/kernel/services/simd.rs"
    - "src/kernel/klib/simd.rs"
  key_links:
    - "src/kernel/core/init.rs -> src/kernel/services/simd.rs via early-init runtime-state ownership"
    - "src/kernel/services/simd.rs -> src/kernel/machine/fpu.rs via typed machine-state initialization"
---

<objective>
Own the low-level machine-state setup needed before MMX/SSE/SSE2 instructions can be considered runtime-safe.

Purpose: Make the runtime state explicit and installed during early init without yet integrating accelerated helper implementations.
Output: Typed machine-state init helpers, service orchestration, and runtime-policy fields that record state ownership.
</objective>

<execution_context>
@~/.codex/get-shit-done/workflows/execute-plan.md
@~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-fpu-mmx-sse-state-ownership/03-CONTEXT.md
@.planning/phases/03-fpu-mmx-sse-state-ownership/03-RESEARCH.md
@docs/simd_policy.md
@src/kernel/core/init.rs
@src/kernel/services/simd.rs
@src/kernel/machine/cpu.rs
@src/kernel/klib/simd.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add typed machine-state initialization and runtime-policy ownership markers</name>
  <files>src/kernel/machine/cpu.rs, src/kernel/machine/fpu.rs, src/kernel/machine/mod.rs, src/kernel/services/simd.rs, src/kernel/core/init.rs, src/kernel/klib/simd.rs</files>
  <read_first>src/kernel/core/init.rs, src/kernel/services/simd.rs, src/kernel/machine/cpu.rs, src/kernel/klib/simd.rs, docs/simd_policy.md</read_first>
  <action>Add a typed machine-state module for CR0/CR4/x87/SSE setup, call it through `services::simd` during early init, and extend the canonical runtime policy so Phase 3 can record that runtime state is owned even while execution stays scalar-first.</action>
  <verify>rg -n "runtime_owned|AccelerationDeferred|MXCSR|OSFXSR|initialize_runtime_state|own_runtime_state" src/kernel/machine src/kernel/services/simd.rs src/kernel/klib/simd.rs src/kernel/core/init.rs</verify>
  <acceptance_criteria>
    - typed machine-state helpers exist in `machine`
    - early init owns runtime state through `services::simd`
    - canonical policy exposes runtime-owned state separately from execution integration
  </acceptance_criteria>
  <done>The repo owns the Phase 3 runtime-state setup without bypassing the approved layer boundaries.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `rg -n "runtime_owned|AccelerationDeferred|MXCSR|OSFXSR|initialize_runtime_state|own_runtime_state" src/kernel/machine src/kernel/services/simd.rs src/kernel/klib/simd.rs src/kernel/core/init.rs`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Early init owns runtime state and the policy records that ownership explicitly
</success_criteria>

<output>
After completion, create `.planning/phases/03-fpu-mmx-sse-state-ownership/03-01-SUMMARY.md`
</output>
