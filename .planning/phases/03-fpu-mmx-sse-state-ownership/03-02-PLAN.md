---
phase: 03-fpu-mmx-sse-state-ownership
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - docs/simd_policy.md
  - docs/kernel_architecture.md
  - src/kernel/klib/simd.rs
autonomous: true
requirements: [HW-04]
user_setup: []
must_haves:
  truths:
    - "The repo documents the current single-task FP/SIMD ownership contract and what is still deferred."
    - "MMX cleanup and SSE exception/control-state expectations are explicit."
  artifacts:
    - "docs/simd_policy.md"
    - "docs/kernel_architecture.md"
    - "src/kernel/klib/simd.rs"
  key_links:
    - "docs/simd_policy.md -> src/kernel/klib/simd.rs via runtime-owned-but-deferred policy contract"
    - "docs/kernel_architecture.md -> src/kernel/services/simd.rs via ownership boundaries"
---

<objective>
Define the current execution-boundary contract for FP/SIMD ownership in this single-task kernel stage.

Purpose: Make it explicit what Phase 3 owns now, what is deferred, and how later phases must treat MMX/SSE/SSE2 state.
Output: Updated docs and policy terminology aligned with the landed code.
</objective>

<execution_context>
@~/.codex/get-shit-done/workflows/execute-plan.md
@~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-fpu-mmx-sse-state-ownership/03-CONTEXT.md
@.planning/phases/03-fpu-mmx-sse-state-ownership/03-RESEARCH.md
@.planning/phases/03-fpu-mmx-sse-state-ownership/03-01-SUMMARY.md
@docs/simd_policy.md
@docs/kernel_architecture.md
@src/kernel/klib/simd.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document the Phase 3 ownership contract and deferred boundaries</name>
  <files>docs/simd_policy.md, docs/kernel_architecture.md, src/kernel/klib/simd.rs</files>
  <read_first>docs/simd_policy.md, docs/kernel_architecture.md, src/kernel/klib/simd.rs</read_first>
  <action>Document the current single-task ownership model, explicit deferred areas such as task-switch preservation and user-mode ABI support, and the MMX/SSE control-state expectations that Phase 4 must respect.</action>
  <verify>rg -n "lazy|MXCSR|OSXMMEXCPT|EMMS|single-task|global ownership|deferred" docs/simd_policy.md docs/kernel_architecture.md src/kernel/klib/simd.rs</verify>
  <acceptance_criteria>
    - the current ownership contract is explicit
    - deferred save/restore and exception handling work is named directly
    - MMX cleanup expectations are written before later implementation phases
  </acceptance_criteria>
  <done>The Phase 3 runtime contract is documented and enforced as the baseline for later acceleration work.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `rg -n "lazy|MXCSR|OSXMMEXCPT|EMMS|single-task|global ownership|deferred" docs/simd_policy.md docs/kernel_architecture.md src/kernel/klib/simd.rs`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The current FP/SIMD ownership model is explicit and bounded
</success_criteria>

<output>
After completion, create `.planning/phases/03-fpu-mmx-sse-state-ownership/03-02-SUMMARY.md`
</output>
