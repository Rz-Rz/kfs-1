---
phase: 04-accelerated-memory-primitives
plan: 03
type: execute
wave: 3
depends_on: ["04-02"]
autonomous: true
requirements: [ACC-02, ACC-03, VER-01]
---

# Phase 4 Plan 03

Implement `memset` on the same owned `Scalar`/`SSE2` dispatch model and close semantic-parity proof coverage.

Must-haves:
- `memset` keeps the scalar contract for zero length, return value, fill pattern, and untouched sentinels.
- The `SSE2` path stays private to the memory family.
- Host and boot tests prove both `SSE2` selection and scalar fallback behavior.
